<ul>
    {% if vote_list|length > 0 %}
    {% for item in vote_list %}
        <li {% if item.timelimit is defined %} class="vote-new" {% endif %}>
            <span class="vote-img">
                {% if (item.sqPath) %}
                <a href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}"><img src="{{ asset(item.sqPath)}}" alt="{{item.title}}"></a>
                {% else %}
                <img src="{{ asset('img/vote/no_image.jpg') }}" height="90" width="90">
                {% endif %}
            </span>
            <span class="vote-txt">
                <div class="title"><a href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}">{{ item.title }}</a></div>
                <div class="con">{% autoescape false %}{{ item.description }}{% endautoescape %}</div>
                <div class="relate corWeak relateVlist">
                    <span>参与人气：<span>{{ item.answerCount }}</span></span><span class="vline"></span>
                    <span>截止日期：{{ item.endTime|date("Y-m-d") }}</span>
                </div>
            </span>
            
            {% if item.timelimit is defined %}
            <div><input type="hidden" name="timelimit_ut" value="{{item.timelimit}}" /><div id="countdown" class="countdown"></div></div>
            <div class="tips expand-btn">
                <span>?</span>
                <div class="tips-con expand-con"><p>倒计时内回答此份快速回答，将额外获得1积分。</p><b class="arrow"></b></div>
            </div>
            {% endif %}

            {% if app.session.has('uid')%}
                {% if(item.endTime|date("Ymd") >= "now"|date('Ymd')) and item.answerable %}
                <a class="vote-btn" href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}">去投票</a>
                {% else %}
                <a class="vote-btn voteDisabled" href="{{ path('jili_frontend_vote_result',{'id':item.id}) }}">去看看</a>
                {% endif %}
            {% else %}
                {% if (item.endTime|date("Ymd") >= "now"|date('Ymd')) %}
                <a class="vote-btn" href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}">去投票</a>
                {% else %}
                <a class="vote-btn voteDisabled" href="{{ path('jili_frontend_vote_result',{'id':item.id}) }}">去看看</a>
                {% endif %}
            {% endif %}
        </li>
    {% endfor %}
{% endif %}
</ul>