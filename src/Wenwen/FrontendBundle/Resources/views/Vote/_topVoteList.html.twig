<ul class="vote">
{% if vote_list|length > 0  %}
    {% for item in vote_list %}
	<li {% if item.timelimit is defined %} class="vote-new" {% endif %}>
		<span class="vote-img">{% if (item.sqPath) %}<a href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}"><img src="{{ asset(item.sqPath)}}" alt="{{item.title}}"></a>{% else %}<img src="{{ asset('img/vote/no_image.jpg') }}" height="90" width="90">{% endif %}</span>
		<span class="survey-item">
			<div class="title"><a href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}">{{ item.title }}</a></div>
			{# <p>{% autoescape false %}{{ item.description }}{% endautoescape %}</p> #}
			<ul {# class="relate corWeak" #}>
				<li><span>问卷类型</span><p>快速问答</p></li>
				<li><span>参与人气</span><p>{{ item.answerCount }}</p></li>
                <li{#  class="time" #}><span>截止日期</span><p>{{ item.endTime|date("Y-m-d") }}</p></li>
				<li><span>积分</span><p class="point"><strong>1</strong></p></li>
			</ul>
			{% if item.timelimit is defined %}
				<div><input type="hidden" name="timelimit_ut" value="{{item.timelimit}}" /><div id="countdown" class="countdown"></div></div>
				<div class="topTip"><span>倒计时内回答，积分加倍</span></div>
				<div class="tips expand-btn">
					<div class="tips-con expand-con"><p>倒计时内回答此份快速回答，将额外获得1积分。</p><b class="arrow"></b></div>
				</div>
			{% endif %}
		</span>
		{% if (item.endTime|date("Ymd") < "now"|date('Ymd')) %}
            <a href="{{ path('jili_frontend_vote_result',{'id':item.id}) }}" class="vote-btn voteDisabled">去看看</a>
        {% else %}
            {% if app.session.has('uid') and item.answerable %}
            <a href="{{ path('jili_frontend_vote_show',{'id':item.id}) }}" class="vote-btn">去投票</a>
            {% else %}
            <a href="{{ path('jili_frontend_vote_result',{'id':item.id}) }}" class="vote-btn voteDisabled">去看看</a>
            {% endif %}
        {% endif %}
	</li>
    {% endfor %}
{% endif %}
</ul>