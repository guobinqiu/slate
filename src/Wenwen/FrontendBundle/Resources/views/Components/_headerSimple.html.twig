<div class="header">
    <div class="header-nav">
        <div class="header-nav-con w1000 clearfix">
            <div class="logo left"><a href="{{ path('_homepage') }}" title="91问问调查网logo"><img src="{{ asset('img/common/logo.svg') }}" alt="91问问调查网logo"></a>{% if theme is defined  %}<span>{{ theme }}</span>{% else %}<span>用户<b>注册</b></span>{% endif %}</div>
            <p class="nav right hiddenTablet"><span>已有91问问账号？</span><a href="{{ path('_user_login') }}">马上登录</a></p>
            {% if app.request.attributes.get('_route') == '_user_resetPwd' %}<p class="navTit hiddenDesktop visiblePhone visibleTablet">重置密码</p>{% endif %}
        </div>
    </div>
</div>