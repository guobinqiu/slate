{% extends '::base.html.twig' %}

{% block body -%}
    <h1>新增</h1>

    <form action="{{ path('gmo_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">保存</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('gmo') }}">
                返回列表
            </a>
        </li>
    </ul>
{% endblock %}
