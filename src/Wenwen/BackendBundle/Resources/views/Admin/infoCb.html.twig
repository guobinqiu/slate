<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<HTML>
<HEAD>
<META http-equiv=Content-Type content="text/html; charset=gb2312">
<LINK href="{{ asset('css/admin.css')}}" type="text/css" rel="stylesheet">
<link href="{{ asset('css/pageNav.css')}}" rel="stylesheet" type="text/css" />
<script src="{{ asset('js/jquery-1.7.js')}}" type="text/javascript"></script>
</HEAD>
<BODY>
<table align="center" width="1024">
    	<tr>
        	<th>公告标题</th>
            <th>公告发布者</th>
            <th>公告接收者</th>
            <th>发表时间</th>
            <!--  <th>公告内容</th>-->
            <th>操作</th>
        </tr>
        {% if pagination %}
         {% for item in pagination %}
        <tr>
          <td align="center">{{ item.title }} </td>
          <td align="center">系统</td>
          <td align="center">全体会员</td>
          <td align="center">{{ item.createtime.date | date('Y-m-d') }}</td>
          <td align="center"><a href="{{ path('_admin_editCb',{'id':item.id}) }}">编辑</a>&nbsp;&nbsp;
          <a href="{{ path('_admin_delCb',{'id':item.id}) }}" onclick="if (confirm('确定要删除吗？')) return true; else return false;">删除</a>
          </td>
        </tr>
         {% endfor %}
         {%  endif %}
    </table>
    {{ knp_pagination_render(pagination) }}

</BODY></HTML>


