<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<HTML>
<HEAD>
<META http-equiv=Content-Type content="text/html; charset=gb2312">
<LINK href="{{ asset('css/admin.css')}}" type="text/css" rel="stylesheet">
<LINK href="{{ asset('css/calendar.css')}}" type=text/css rel=stylesheet>
<script src="{{ asset('js/jquery-1.7.js')}}" type="text/javascript"></script>
<SCRIPT src="{{ asset('js/load.js')}}" type=text/javascript></SCRIPT>
<script src="{{ asset('js/My97DatePicker/WdatePicker.js')}}" type="text/javascript"></script>
<script src="{{ asset('js/kindeditor/kindeditor.js')}}" type="text/javascript"></script>
<script type="text/javascript">
$(document).ready(function(){
    
});
    KE.show({
        id : 'info'
    });
    KE.show({
        id : 'rule'
    });

</script>
</HEAD>
<BODY>
<table align="center" width="1024">
      <tr>
          <th>活动标题</th>
            <th>活动开始时间</th>
            <th>活动截至时间</th>
            <th>活动图片</th>
            <th>操作</th>
            <th>状态</th>
        </tr>
        {% if pagination %}
         {% for item in pagination %}
        <tr>
          <td align="center">{{ item.businessName }} </td>
          <td align="center">{{ item.startTime.date | date('Y-m-d') }}</td>
          <td align="center">{{ item.endTime.date | date('Y-m-d') }}</td>
          <td align="center"><img src="{{ asset(item.activityImage)}}"/></td>
          <td align="center"><a href="{{ path('_admin_editBusinessActivity',{'id':item.id}) }}">编辑</a>&nbsp;&nbsp;
          <a href="{{ path('_admin_delBusinessActivity',{'id':item.id}) }}" onclick="if (confirm('确定要删除吗？')) return true; else return false;">删除</a>
        </td>
           <td align="center">
           {% if date(item.startTime) > date()  %}
                未开始 
           {% else %} 
                {% if date(item.endTime) < date() %}
                    已停止
                {% else %}
                    投放中
                {% endif %}
           {% endif %}
           </td>
        </tr>
         {% endfor %}
         {%  endif %}
    </table>
    {{ knp_pagination_render(pagination) }}

</BODY></HTML>


