exchange_id,91jili_email,payment_amount,payment_point,status,reason
{% if exchange %}
{% for item in exchange %}{{ item.wenwenExchangeId }},{{ item.email }},{{ item.paymentPoint }},{{ item.paymentPoint }},{% if item.status == 1 %}ok{% else %}ng{% endif %},{{ item.reason }}
{% endfor %}
{% endif %}