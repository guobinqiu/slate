<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<HTML>
<HEAD>
<META http-equiv=Content-Type content="text/html; charset=gb2312">
<LINK href="{{ asset('css/admin.css')}}" type="text/css" rel="stylesheet">
<link href="{{ asset('css/pageNav.css')}}" rel="stylesheet" type="text/css" />
<script src="{{ asset('js/jquery-1.7.js')}}" type="text/javascript"></script>
</HEAD>
<BODY>
<form action="{{ path('_admin_cardInfo')}}" method="post" id="form1"}} >
<div align="center" style="margin-top:30px;">用户id或用户名搜索:<input type="text" name="user_flag" value="{{ userFlag }}" /><input type="submit" name="search" value="搜 索"/></div>
</form>
{% if code %}
 <center><font color='red'>请输入用户id或用户名</font></center>
{% else %}
{% endif %}
{% if pagination %}
<table align="center" width="1024">
<tr><td align="center" colspan="6"><h3>剩余条数:{{ card_remain }}</h3></td></tr>
    	<tr>
            <th>用户id</th>
        	  <th>用户名</th>
            <th>获得的条数</th>
            <th>发放的条数</th>
            <th>发放的积分</th>
            <th>时间</th>
        </tr>
         {% for item in pagination %}
        <tr>
          <td align="center">{{ item.userId }} </td>
          <td align="center">{{ item.email }} </td>
          <td align="center">{{ item.matchCount }}</td>
          <td align="center">{{ item.rewardCount }}</td>
          <td align="center">{{ item.rewardPoint }}</td>
          <td align="center">{{ item.createTime.date | date('Y-m-d') }}</td> 
        </tr>
         {% endfor %}
        
    </table>
    {{ knp_pagination_render(pagination) }}
 {%  endif %}
</BODY></HTML>


