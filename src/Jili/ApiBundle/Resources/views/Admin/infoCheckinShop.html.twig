<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN">
<HTML>
<HEAD>
<META http-equiv=Content-Type content="text/html; charset=gb2312">
<LINK href="{{ asset('css/admin.css')}}" type="text/css" rel="stylesheet">
<LINK href="{{ asset('css/calendar.css')}}" type=text/css rel=stylesheet>
<script src="{{ asset('js/lib/jquery-1.11.1.min.js')}}" type="text/javascript"></script>
<SCRIPT src="{{ asset('js/plugin/load.js')}}" type=text/javascript></SCRIPT>
<script src="{{ asset('js/plugin/My97DatePicker/WdatePicker.js')}}" type="text/javascript"></script>
<script src="{{ asset('js/kindeditor/kindeditor.js')}}" type="text/javascript"></script>
<script type="text/javascript">
$(document).ready(function(){
    
});
    KE.show({
        id : 'info'
    });
    KE.show({
        id : 'rule'
    });

</script>
</HEAD>
<BODY>
<table align="center" width="1024">
      <tr>
          <th>商店名</th>
            <th>间隔(天数)</th>
            <th>创建时间</th>
            <th>操作</th>
        </tr>
        {% if pagination %}
         {% for item in pagination %}
        <tr>
          <td align="center">{{ item.title }} </td>
          <td align="center">{{ item.interSpace }}</td>
          <td align="center">{{ item.createTime.date | date('Y-m-d H:i:s') }}</td>
          <td align="center"><a href="{{ path('_admin_editCheckinShop',{'id':item.id}) }}">编辑</a>&nbsp;&nbsp;
          <a href="{{ path('_admin_delCheckinShop',{'id':item.id}) }}" onclick="if (confirm('确定要删除吗？')) return true; else return false;">删除</a>
        </td>
        </tr>
         {% endfor %}
         {%  endif %}
    </table>
    {{ knp_pagination_render(pagination) }}

</BODY></HTML>


