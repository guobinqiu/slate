{% extends 'JiliFrontendBundle::base.html.twig' %}
{% block title %}积粒网-体验广告{% endblock %}

{% block stylesheet %}

<link href="{{ asset('css/aside.css')}}" rel="stylesheet" type="text/css" />
<link href="{{ asset('css/adListNew.css')}}" rel="stylesheet" type="text/css" />
{% endblock %}

{% block javascript %}
<script src="{{ asset('js/adListNew.js')}}" type="text/javascript"></script>
{% endblock %}

{% block body %}
<div class="webArticle adList">
  <article {% if ads %} {% else %} {% endif %}>
    <!--  subnav  -->
    <div class="offwowSubnav">
        <ul class="subnavList">
            <li class="subnavItem first"><a class="now" href="{{ asset(path('_advertiserment_list')) }}">任务大厅一</a></li>
            <li class="subnavItem"><a href="{{ asset(path('_advertiserment_offer99')) }}">任务大厅二</a></li>
            <li class="subnavItem"><a href="{{ asset(path('_advertiserment_bangwoya')) }}">任务大厅三</a></li>
        </ul>
        <div class="navNew"><img src="{{ asset('images/newTag.gif') }}" title="NEW"></div>
    </div>
    <!--  subnav  /-->
    <iframe src='http://www.offer-wow.com/affiliate/wall/open.do?websiteid={{  offerwow_com.websiteid }}&styleIndex=1&memberid={{  app.session.get('uid') }}' style='width:720px;height:560px;' scrolling='no' frameborder='no' border='0' allowTransparency='true'></iframe>
    <div class="explain"><a href="#" class="explainBtn">活动说明</a>
        <div class="explainMore">
            <p>1、点击【马上参加】，仔细阅读【任务步骤】，按要求一步步完成即可。</p>
            <p>2、即时返利做完立刻奖励，非即时返利审核通过后奖励。</p>
            <p>3、如对活动有问题，点击【我的任务历史】下的【在线咨询】即可！</p>
            <p style="border-bottom:none;">4、活动不断更新，敬请多多关注！</p>
        </div>
    </div>

  </article>

  {% if ads %}
  <aside class="offowAside">
    <div class="cpaMain">
        <div class="cpaAll">

     {% for item in ads %}
            <div class="cpa{{ loop.last ? ' last' : '' }}">
              <dd><a href="{{ path('_advertiserment_index',{'id':item.id}) }}"><img src="{{ asset(item.listImage) }}" width="130px" height="95px" alt="{{item.title}}"></a></dd>

                      {% if item.incentiveType==1 %}
                         <dt>+{{ item.incentive }}米粒<br/>
                        {% elseif item.incentiveType==2 %}
                         <dt>{{ item.incentiveRate/10000 }}%<br />最高获消费额<br/>
                         {% else %}
                     {% endif %}
              <a class="joinBtn" href="{{ path('_advertiserment_index',{'id':item.id}) }}">马上参与</a>
              <a href="{{ path('_advertiserment_index',{'id':item.id}) }}" class="title">{{ item.title  }}</a></dt>
            </div>
     {% endfor %}
        </div>
    </div>
  </aside>
  {% else %}
  <aside class="offowAside">
      <div>
      	<a data-type="3" data-tmpl="290x380" data-tmplid="30" data-rd="2" data-style="2" data-border="1" href="#"></a>
      </div>
  </aside>
  {% endif %}
</div>
{% endblock %}
