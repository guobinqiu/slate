{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GameSeekerPointsPool creation</h1>

    <form action="{{ path('admin_game-seeker_points-pool_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_game-seeker_points-pool') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
